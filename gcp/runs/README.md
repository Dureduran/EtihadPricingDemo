`ingest_run.json` records a Colab run against project `mymain-496711` (`"runtime": "gcp"`).

Tables: `mymain-496711.ancillary_lab.offer_log_raw` and `mymain-496711.ancillary_lab.offer_log_train`.

Do not hyperlink the GCP console, Colab, or a Vertex Workbench instance.

The Streamlit lab still scores from `new_model/model.joblib`. Databricks and GCP share `new_model/train.py` and the same offer log.
